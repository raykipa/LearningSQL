#Learning to use COUNT
COUNT(*) counts all rows
COUNT counts number of distinct values in a column
#1. Count the number of rows in the people table.
SELECT COUNT(*)
FROM people;

#2. Count the number of (non-missing) birth dates in the people table.
SELECT COUNT(birthdate)
FROM people;


#Using the "where" command
counting movies that were released before 2000
SELECT COUNT(*)
FROM films
WHERE release_year < 2000

SELECT title, release_year
FROM films
WHERE release_year > 2000

#Selecting a person with a particular birthdate
SELECT name, birthdate
FROM people
WHERE birthdate = '1974-11-11'
